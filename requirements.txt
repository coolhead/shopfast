fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.10
redis==5.0.8
boto3==1.35.24

# DevSecOps tools
ruff==0.6.9
bandit==1.7.9
safety==2.3.5
pytest==8.3.3

# Explicitly upgrade vulnerable transitive deps to safe versions
starlette>=0.40.0   # fixes both starlette vulns
python-jose[cryptography]>=3.4.0  # fixes both python-jose vulns
python-multipart>=0.0.18         # fixes DoS vuln
ecdsa>=0.19.0                    # no fix exists, but we don't use ECDSA directly
